{
  "frontend/src/index.css": "Global styles with warm earthy OKLCH color palette, Zilla Slab and Caveat font imports, utility classes for card wobble animations, text shadows, amber glow effects, and new `quote-cycle`/`quote-visible`/`quote-hidden` transition classes for the cycling quote display.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Zilla Slab/Caveat font families, warm earthy color aliases (amber, cream, espresso), custom box shadows, and fade-in-up/wiggle keyframe animations.",
  "frontend/src/hooks/useQueries.ts": "React Query hook `useGetQuotes` that fetches quotes from the backend actor, enabled as soon as the actor is available (no longer blocked by `isFetching`), with 3 retries and exponential backoff up to 30s.",
  "frontend/src/components/QuoteCard.tsx": "Individual quote card component that accepts a `Quote` object (with a `text` field) and renders `quote.text`, with decorative quotation marks, accent symbols, numbered badges, and wobble transforms.",
  "frontend/src/components/QuoteCardSkeleton.tsx": "Loading skeleton placeholder for quote cards using `bg-espresso-light` skeleton blocks and `border-amber-warm/40` border for theme-consistent visibility.",
  "frontend/src/pages/QuotesPage.tsx": "Main quotes page that cycles through quotes every 4.5s with fade transitions; now shows the skeleton loading UI during both initial load and all retry attempts (`isPending || isFetching`), only revealing the error screen after all retries are exhausted (`isError && !isFetching`).",
  "frontend/src/App.tsx": "Root app component that wraps `QuotesPage` in an `ErrorBoundary` so runtime errors show a fallback instead of a blank page, with the Footer always rendered outside the boundary.",
  "frontend/src/components/ErrorBoundary.tsx": "React class-based error boundary that catches runtime render errors from child components and displays a warm earthy fallback UI with a \"try again\" button, preventing blank screens from uncaught exceptions."
}